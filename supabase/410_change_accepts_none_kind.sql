-- 410: Платёж со сдачей падал с «Сдача относится к блоку баланса участника».
-- В миграции 408 сдача перестала относиться к блоку (она возвращает деньги,
-- не зачтённые ни на один блок), и клиент стал слать participant_balance_kind
-- = none — а входная валидация в fin_create_payment этот none всё ещё
-- отвергала. Запрет снят: блок сдачи не проверяется, в проводку по-прежнему
-- пишется none.
-- Полный текст патча применён через MCP apply_migration, см. историю миграций.
