-- 409: Переплата одного блока гасит долг другого (ВГ, 25.08: «и долг, и аванс,
-- итог верный, но почему-то оргвзнос как долг»). Это деньги одного человека за
-- один ретрит: требовать доплату при собственной переплате нельзя.
--
-- В fin_private_participant_balance после зачёта «общих» платежей добавлен
-- взаимозачёт между блоками: аванс блока-донора гасит долг блока-получателя,
-- сколько зачтено — в поле blocks.<блок>.offset («зачтено из аванса» в карточке).
-- Итог участника (net) не меняется, меняется только раскладка по блокам.
-- Сухой прогон по Сева-ретриту: затронуто 6 карточек, net не изменился ни у кого.
-- Полный текст патча применён через MCP apply_migration, см. историю миграций.
