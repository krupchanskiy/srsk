-- 398: автоначисление из CRM пишет брутто и скидку раздельно (ошибка ВГ, 22.08).
-- Применена через MCP. fin_sync_charges_from_crm: amount = стандартная цена,
-- discount_amount = стандартная − итог по условиям (к оплате = разница, как
-- везде в fin_charges). «Без оргвзноса» → 25 000 − 25 000 = 0 вместо
-- нарушения fin_charges_discount_range. Фикс-сумма в валюте переводится в ₹
-- по курсу ретрита (леджер начислений рупиевый), курс записан в описание.
