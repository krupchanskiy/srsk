-- Операция с датой в будущем принималась — и деньги исчезали из виду.
--
-- Проверено на живых данных (в откате): пополнили кассу на 50 000, списали
-- 30 000 датой 2027-09-01. Результат:
--   • остаток кассы стал 20 000 ПРЯМО СЕЙЧАС (остаток считается по всем
--     проводкам без учёта даты);
--   • в отчёте за последний месяц этого расхода НЕТ вообще.
-- То есть опечатка в годе уводит деньги из кассы сегодня, не появляясь ни в
-- одном отчёте за период. При сверке касса не сойдётся, и объяснить разницу
-- будет нечем.
--
-- Исправление: запрет на дату операции позже CURRENT_DATE + 1.
-- Запас в один день обязателен: база живёт в UTC, ашрам в Мумбаи (UTC+5:30),
-- поэтому «сегодня» по-местному может быть CURRENT_DATE + 1 по UTC.
-- Задним числом по-прежнему разрешено — это нужно для «забыли записать».
--
-- Правка внесена патчем по месту в fin_private_create_flow: сразу после
--   v_on := fin_private_get_date(payload, 'occurred_on', true);
-- добавлено
--   IF v_on > CURRENT_DATE + 1 THEN
--     RAISE EXCEPTION 'occurred_on_in_future' USING DETAIL = format(
--       'Дата операции %s — в будущем. Похоже на опечатку в годе: такая
--        операция сразу уменьшит остаток счёта, но не попадёт ни в один
--        отчёт за период.', v_on);
--   END IF;
--
-- Почему не в fin_private_get_date: та функция IMMUTABLE, внутри неё
-- CURRENT_DATE недоступен, и она же разбирает даты полей, где будущее законно.
--
-- Актуальное определение:
--   SELECT pg_get_functiondef('fin_private_create_flow(fin_operation_type,jsonb)'::regprocedure);

-- Инвариант на случай, если запись пройдёт мимо RPC (прямой SQL, миграция).
-- Добавлен в fin_run_integrity_checks как operation_in_future:
--   SELECT count(*) FROM fin_operations WHERE occurred_on > CURRENT_DATE + 1
-- Проверок стало 16.
