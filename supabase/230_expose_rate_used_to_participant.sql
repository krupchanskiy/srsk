-- Курс конвертации виден участнику и администратору.
-- Курс фиксируется в момент проведения (fin_postings.rate_used, защищён
-- триггером fin_postings_guard от правки задним числом) — но наружу не отдавался.
-- Гость видел «€ 100» без рупиевого эквивалента и без курса, поэтому вопрос
-- «а почему у меня столько рупий долга» упирался в переписку с администратором.
-- Теперь расчёт самодокументирован: сумма, курс на дату операции, результат.
--
-- Изменения: 'rate_used' добавлен в fin_private_participant_payments и прокинут
-- в portal_fin_get_my_finances (своя детализация + семейная при full_details).
-- Полный текст обеих функций — в применённой миграции на проде.

INSERT INTO translations (key, ru, en, hi) VALUES
  ('fin_at_rate', 'по курсу', 'at rate', 'दर पर')
ON CONFLICT (key) DO UPDATE SET ru = EXCLUDED.ru, en = EXCLUDED.en, hi = EXCLUDED.hi;
