-- =============================================================
-- Финмодуль, Этап 3а — тесты участников (P1-P17)
-- Запуск: батчем в BEGIN/ROLLBACK (см. git-историю коммита этапа 3а —
-- полный текст прогона в истории сессии; повторный запуск делается
-- копией блока из описания ниже).
--
-- Покрытие (числовые примеры — прямо из ТЗ раздела 7):
--   P1  пачка начислений 15000+35000+10000 → долг 60000; quick==net
--   P2  идемпотентный повтор пачки начислений (сценарий 101)
--   P3  ТЗ пример 1: платёж 10000 (accommodation) + general 500$@83.5 →
--       org 0, acc 0, meals 8250, total_debt 8250, advance 0
--   P4  ТЗ пример 2: extra 5000 + general 5000 → сальдо 0, не ложный
--       аванс (сценарий 38)
--   P5  ТЗ пример 3: opening debt general 10000 → «Общий долг» 10000
--       (сценарий 39)
--   P6  повтор загрузки реестра → skipped; изменённая строка →
--       cutover_row_conflict (сценарии 49, 14-appendix)
--   P7  возврат 40$ из 500$: base пропорционально 3340, rate
--       наследуется (сценарий 7)
--   P8  возврат сверх остатка → refund_amount_exceeds_available
--   P9  сторно возврата → available снова 500; полный возврат добирает
--       точный base 41750 (сценарии 78, 85)
--   P10 сторно платежа с активным возвратом → отказ (сценарий 22)
--   P11 сторно платежа восстанавливает долг (сценарий 22)
--   P12 отмена начисления: пересчёт долга, no-op повтор, конфликт
--       другой причины (сценарий 20)
--   P13 групповой платёж за двоих одной операцией (сценарий 3)
--   P14 коррекция opening-позиции + идемпотентность (сценарии 84,92,105)
--   P15 fin_list_retreat_participants
--   P16 статусы платежей (refunded_fully и др.)
--   P17 portal_fin_get_my_finances под участником (viewer из auth.uid())
-- Все ассерты включают проверку равенства подробной и быстрой формул.
-- =============================================================
