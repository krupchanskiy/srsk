-- 408: Сдача не уменьшает блок участника (ВГ, 25.08: «человек всё оплатил,
-- а по итогу долг ₹3 600»). В модели v4 строки платежа несут ровно то, что
-- зачисляется на блоки, а сдача возвращает излишек из «Получено от гостя» —
-- значит баланса участника она касаться не должна, только кассы.
--
-- 1. fin_postings_validate: у out-проводки платежа (статья «Сдача участнику»)
--    обязателен только участник, блок баланса не требуется.
-- 2. fin_create_payment пишет сдачу с participant_balance_kind = 'none'.
-- 3. Данные: привязка снята только там, где сдача создала долг (ввод по модели
--    v4 — Александр Казаков ₹3 600, Лаванга-латика 20 €). У более ранних сдач,
--    введённых старым способом (в строки платежа заводили всю полученную сумму),
--    привязка к блоку осмысленна и оставлена как есть.
-- Полный текст применён через MCP apply_migration, см. историю миграций.
