# ШРСК — Sri Rupa Seva Kunja

> Система управления ашрамом в Индии

**Production:** https://in.rupaseva.com
**Repository:** https://github.com/krupchanskiy/srsk
**Deployment:** GitHub Pages (auto-deploy from main)

**Техническая архитектура:** [SRSK-TECH-ARCHITECTURE.md](./SRSK-TECH-ARCHITECTURE.md)

---

## Бизнес-контекст

### Что это?

ШРСК — веб-приложение для управления духовным ашрамом в Индии. Автоматизирует ключевые процессы:

- **Проживание гостей** — бронирования, заезды/выезды, трансферы
- **Кухня** — меню, рецепты, расчёт продуктов
- **Склад** — остатки, заявки, приёмка, выдача
- **Ретриты** — мероприятия с регистрацией гостей
- **Команда** — сотрудники ашрама, служения, отделы

### Для кого?

| Роль | Задачи |
|------|--------|
| **Ресепшен** | Заселение, выезды, шахматка, уборка |
| **Повар** | Меню, рецепты, заявки на продукты |
| **Кладовщик** | Остатки, приёмка, выдача, ревизия |
| **Организатор** | Создание ретритов, регистрация гостей |
| **Администратор** | Настройки, переводы, пользователи |

### Уникальные особенности

1. **Мультиязычность** — RU / EN / HI (хинди с девангари)
2. **Динамические локации** — несколько кухонь (основная, кафе, гостевая)
3. **Цветовое кодирование** — Kitchen (#f49800), Housing (#8b5cf6)
4. **No build step** — чистый HTML/JS, деплой без сборки

---

## Модули системы

### 1. Kitchen (Кухня) 🍳

**Цвет:** `#f49800` (оранжевый)

#### Кухня
| Страница | Путь | Назначение |
|----------|------|------------|
| Меню | `kitchen/menu.html` | Планирование блюд на день/неделю/месяц |
| Шаблоны меню | `kitchen/menu-templates.html` | Сохранённые недельные меню |
| Рецепты | `kitchen/recipes.html` | База рецептов (129 шт, 12 категорий) |
| Рецепт | `kitchen/recipe.html` | Карточка рецепта с ингредиентами |
| Редактор | `kitchen/recipe-edit.html` | Создание/редактирование рецепта |
| Продукты | `kitchen/products.html` | Справочник продуктов |
| Справочники | `kitchen/dictionaries.html` | Категории, единицы измерения |

#### Категории рецептов (12)
- Суп (12), Салат (9), Напиток (0), Сладость (20)
- Закуска (20), Чатни/Райты/Соусы (16), Каши (15)
- Фрукты (4), Солёная выпечка (8), Овощные блюда (18), Сладкая выпечка (7)

#### Склад
| Страница | Путь | Назначение |
|----------|------|------------|
| Остатки | `stock/stock.html` | Текущие остатки на складе |
| Заявки | `stock/requests.html` | Заявки от кухни на закупку |
| Приёмка | `stock/receive.html` | Оприходование товара |
| Выдача | `stock/issue.html` | Выдача продуктов на кухню |
| Инвентаризация | `stock/inventory.html` | Ревизия остатков |
| Настройки | `stock/stock-settings.html` | Минимальные остатки, категории |

---

### 2. Housing (Проживание) 🏠

**Цвет:** `#8b5cf6` (фиолетовый)

#### Вайшнавы
| Страница | Путь | Назначение |
|----------|------|------------|
| Все | `vaishnavas/index.html` | Общая база людей |
| Гости | `vaishnavas/guests.html` | Только гости ретритов |
| Команда | `vaishnavas/team.html` | Сотрудники ашрама |
| Профиль | `vaishnavas/person.html` | Карточка вайшнава |

#### Размещение
| Страница | Путь | Назначение |
|----------|------|------------|
| Распределение | `vaishnavas/retreat-guests.html` | Импорт гостей из CSV |
| Предварительное | `vaishnavas/preliminary.html` | Назначение комнат до заезда |
| Заезды | `placement/arrivals.html` | Ожидаемые прибытия |
| Выезды | `placement/departures.html` | Планируемые отъезды |
| Трансферы | `placement/transfers.html` | Заказ такси |

#### Ресепшен
| Страница | Путь | Назначение |
|----------|------|------------|
| Шахматка | `placement/timeline.html` | Timeline всех комнат |
| Бронирования | `placement/bookings.html` | Список бронирований |
| Уборка | `reception/cleaning.html` | График уборки |
| Планы этажей | `reception/floor-plan.html` | SVG-схемы зданий |

#### Настройки
| Страница | Путь | Назначение |
|----------|------|------------|
| Здания | `reception/buildings.html` | Постоянные и временные |
| Комнаты | `reception/rooms.html` | Номера, типы, вместимость |
| Справочники | `reception/dictionaries.html` | Типы комнат, категории |

---

### 3. CRM (Продажи) 💼

**Цвет:** `#10b981` (изумрудный)

| Страница | Путь | Назначение |
|----------|------|------------|
| Дашборд | `crm/dashboard.html` | Воронка продаж, статистика |
| Все заявки | `crm/deals.html` | Список сделок с фильтрами |
| Карточка сделки | `crm/deal.html` | Детали, комментарии, история |
| Мои задачи | `crm/tasks.html` | Задачи менеджера |
| Лог активности | `crm/activity-log.html` | История действий |
| Форма заявки | `crm/form.html` | Публичная форма регистрации |
| Шаблоны | `crm/templates.html` | Шаблоны сообщений |
| Теги | `crm/tags.html` | Теги для сделок |
| Услуги | `crm/services.html` | Услуги и цены |
| Валюты | `crm/currencies.html` | Курсы валют (INR) |
| Менеджеры | `crm/managers.html` | Менеджеры ретритов, очередь |

---

### 4. Guest Portal (Личный кабинет гостя) 👤

**URL:** https://in.rupaseva.com/guest-portal/

Отдельный модуль с собственным дизайном (Tailwind без DaisyUI).

| Страница | Путь | Назначение |
|----------|------|------------|
| Dashboard | `guest-portal/index.html` | Профиль + ретрит + трансферы |
| Логин | `guest-portal/login/` | Вход для гостей |
| Контакты | `guest-portal/contacts.html` | Контакты ашрама |
| Материалы | `guest-portal/materials.html` | Гайды и материалы |
| Ретриты | `guest-portal/retreats.html` | История ретритов |

---

### 5. Admin (Управление) ⚙️

**Цвет:** `#374151` (тёмно-серый)
**Доступ:** Только суперпользователи

| Страница | Путь | Назначение |
|----------|------|------------|
| Ретриты | `ashram/retreats.html` | Создание/редактирование ретритов |
| Праздники | `ashram/festivals.html` | Календарь праздников |
| Переводы | `settings/translations.html` | i18n (RU/EN/HI) |
| Пользователи | `settings/user-management.html` | Роли и права доступа |

---

## Бизнес-процессы

### 1. Цикл ретрита

```
1. Создание ретрита
   └─ ashram/retreats.html
   └─ Название, даты, ожидаемое количество, цвет

2. Регистрация гостей
   └─ vaishnavas/retreat-guests.html
   └─ Импорт CSV из Google Forms
   └─ Автоматический матчинг по email/телефону

3. Предварительное распределение
   └─ vaishnavas/preliminary.html
   └─ Назначение комнат до приезда

4. Планирование трансферов
   └─ placement/arrivals.html
   └─ Просмотр времени прилёта
   └─ Заказ такси

5. Заселение
   └─ placement/timeline.html
   └─ Отметка прибытия в шахматке

6. Проживание
   └─ reception/cleaning.html
   └─ Уборка, переселение, продление

7. Выезд
   └─ placement/departures.html
   └─ Такси, освобождение комнаты
```

### 2. Цикл кухни

```
1. Планирование меню
   └─ kitchen/menu.html
   └─ Выбор блюд на день/неделю

2. Расчёт продуктов
   └─ Автоматически по рецептам
   └─ Учёт количества едоков

3. Создание заявки
   └─ stock/requests.html

4. Закупка
   └─ Печать заявки, поездка на рынок

5. Приёмка
   └─ stock/receive.html

6. Выдача на кухню
   └─ stock/issue.html

7. Готовка
   └─ kitchen/recipe.html
```

### 3. Складской цикл

```
Мониторинг → Заявка → Закупка → Приёмка → Хранение → Выдача → Ревизия
```

---

## Модель данных

### Ключевые таблицы

#### Люди
```sql
vaishnavas
├── id, name, surname
├── spiritual_name          -- духовное имя
├── email, phone
├── country, city
├── spiritual_teacher       -- духовный учитель
├── is_team_member          -- команда?
├── department_id           -- FK → departments
├── service                 -- служение
└── senior_id               -- FK → vaishnavas (старший)

vaishnava_stays             -- периоды пребывания команды
├── vaishnava_id
├── start_date, end_date
└── reason

departments                 -- отделы команды
├── id
└── name_ru, name_en, name_hi
```

#### Проживание
```sql
buildings
├── id, name_ru, name_en, name_hi
├── building_type_id
├── is_temporary            -- временное здание?
├── available_from, available_until
└── sort_order              -- 1-10 постоянные, 50+ временные

rooms
├── id, number
├── building_id             -- FK → buildings
├── room_type_id
├── capacity
└── floor

bookings
├── id
├── room_id                 -- FK → rooms
├── check_in, check_out
├── status
└── notes

residents
├── id
├── booking_id              -- FK → bookings
├── vaishnava_id            -- FK → vaishnavas
├── room_id                 -- NULL = самостоятельное проживание
└── category_id
```

#### Ретриты
```sql
retreats
├── id
├── name_ru, name_en, name_hi
├── description_ru, description_en
├── start_date, end_date
├── expected_count
└── color                   -- hex цвет

retreat_registrations
├── id
├── retreat_id              -- FK → retreats
├── vaishnava_id            -- FK → vaishnavas
└── status

guest_transfers
├── id
├── vaishnava_id
├── retreat_id
├── direction               -- 'arrival' | 'departure'
├── flight_datetime
├── needs_transfer          -- 'yes' | 'no'
├── taxi_status             -- 'ordered' | NULL
├── taxi_driver_info
└── taxi_ordered_by
```

#### Кухня
```sql
recipes
├── id
├── name_ru, name_en, name_hi
├── category_id             -- FK → recipe_categories
├── output_amount, output_unit
├── portion_size, portion_unit
├── cooking_time            -- минуты
└── instructions

recipe_ingredients
├── recipe_id               -- FK → recipes
├── product_id              -- FK → products
├── quantity
└── unit

products
├── id
├── name_ru, name_en, name_hi
├── category_id             -- FK → product_categories
├── unit                    -- базовая единица
└── min_stock               -- минимальный остаток

stock
├── id
├── product_id              -- FK → products
├── location_id             -- FK → locations
├── quantity
└── updated_at

menu_days
├── id
├── date
├── location_id
└── created_by

menu_items
├── id
├── menu_day_id             -- FK → menu_days
├── recipe_id               -- FK → recipes
├── meal_type               -- завтрак/обед/ужин
└── portions                -- количество порций
```

#### i18n
```sql
translations
├── key                     -- уникальный ключ
├── ru
├── en
└── hi
```

---

## Известные проблемы

### Технические (общие)

| Проблема | Описание | Решение |
|----------|----------|---------|
| **Конфликт переменной t** | layout.js обёрнут в IIFE, но const t может конфликтовать | Использовать `const t = key => Layout.t(key);` после загрузки |
| **RLS Supabase** | update/insert с `.select().single()` может вернуть ошибку | Использовать `.select()` и брать `result.data?.[0]` |
| **Storage RLS** | Ошибка row-level security при загрузке файлов | Проверить RLS-политики для storage.objects |
| **Кэш переводов** | После добавления новых переводов кэш устаревает | `Cache.invalidate('translations')` или Ctrl+Shift+R |
| **Browser bfcache** | При навигации "Назад" страница из кэша без JS | Использовать `pageshow` listener |
| **N+1 запросы** | Циклы с await создают множественные запросы | Использовать `.in()` и группировать на клиенте |

### Технический долг (требует рефакторинга)

| Проблема | Описание | Приоритет | Статус |
|----------|----------|-----------|--------|
| ~~RLS хаос~~ | ~~163 политики~~ | ~~🔴 ВЫСОКИЙ~~ | ✅ Решено (82 политики после консолидации) |
| ~~Inline onclick~~ | ~~756 inline обработчиков~~ | ~~🟡 Средний~~ | ✅ Решено (вынесено в js/pages/) |
| ~~Монолитные HTML~~ | ~~timeline.html 2,600+ строк~~ | ~~🟡 Средний~~ | ✅ Решено (разбито на HTML + JS) |
| **Зависимость от CDN** | Tailwind, DaisyUI, Supabase грузятся из интернета | 🟢 Низкий | Опционально: Vite + npm |
| **Playwright тесты** | Тесты начаты, но покрытие неполное | 🟡 Средний | В процессе |

> **Статус системы:** ШРСК — это **рабочая production-система** с 78 страницами, 107+ миграциями и реальными пользователями. Основной технический долг закрыт (RLS консолидирован, код вынесен в js/pages/). См. [Техническую архитектуру](./SRSK-TECH-ARCHITECTURE.md) для плана эволюционного развития.

### Бизнес-ограничения

| Проблема | Приоритет | Решение | Статус |
|----------|-----------|---------|--------|
| **Нет уведомлений** | 🔴 Высокий | Supabase Edge Functions + Telegram | 🔜 Планируется |
| **Ручной расчёт порций** | 🔴 Высокий | Связь меню с bookings | 🔜 Планируется |
| **Ручной импорт CSV** | 🟡 Средний | Google Forms API | 🔜 Планируется |
| ~~Нет realtime~~ | ~~🟢 Низкий~~ | ~~Supabase Realtime~~ | ✅ Реализовано (timeline, preliminary, stock) |

### Что НЕ является приоритетом (для стартапа)

| "Проблема" | Почему не приоритет |
|------------|---------------------|
| ~~Мобильное приложение~~ | Веб с DaisyUI/Tailwind адаптивный, работает на телефонах. PWA/нативное — оверкилл |
| ~~Модуль финансов~~ | Огромный объём работы. Используйте 1С / Excel / специализированное ПО |
| ~~Дашборд аналитики~~ | Выгрузка в Excel решает 90% задач. Красивые графики — позже |
| ~~Личный кабинет гостя~~ | ✅ **Реализовано!** См. Guest Portal (/guest-portal/) |

---

## Возможности развития

### Quick Wins (можно сделать за 1 день)

| Фича | Время | Польза | Статус |
|------|-------|--------|--------|
| **Telegram-уведомления** | 3-4 часа | Ресепшен/кухня узнают сразу | 🔜 |
| **Авторасчёт порций** | 1-2 часа | Повар не считает вручную | 🔜 |
| ~~Supabase Realtime~~ | ~~2-3 часа~~ | ~~Синхронизация вкладок~~ | ✅ Готово |

### Улучшения (средний срок)

| Фича | Время | Когда нужно |
|------|-------|-------------|
| **Vite + TypeScript** | 2-3 дня | Уход от CDN, нормальная сборка, типизация |
| **Google Forms API** | 3-4 часа | Автоматический импорт вместо CSV |
| **Публичное меню** | 2-3 часа | Страница для гостей без авторизации |

### Не планируем

- ~~Нативное приложение~~ — веб адаптивный, работает на телефонах
- ~~Полноценная аналитика~~ — Excel достаточно
- ~~Модуль финансов~~ — отдельное ПО (1С/Excel)
- ~~Offline-режим~~ — данные должны быть актуальными
- ~~Миграция на Rails~~ — текущий стек справляется (см. [критерии](./SRSK-TECH-ARCHITECTURE.md#когда-реально-нужен-rails))

---

## Техническая архитектура

### Стек

- **Frontend:** Vanilla JS + DaisyUI + Tailwind CSS (CDN)
- **Backend:** Supabase (PostgreSQL + Auth + Realtime)
- **Fonts:** Noto Sans + Noto Sans Devanagari
- **Deployment:** GitHub Pages

### Ключевые файлы

```
js/
├── config.js           # Supabase credentials
├── layout.js           # Единая точка входа (хедер, навигация, i18n)
├── cache.js            # localStorage кэширование
├── utils.js            # Утилиты (валидация)
├── date-utils.js       # Работа с датами (локальное время!)
├── modal-utils.js      # Утилиты для модальных окон
├── vaishnavas-utils.js # Общие функции для списков людей
├── crm-utils.js        # Утилиты CRM (иконки, форматирование)
├── color-init.js       # Предотвращение FOUC
├── auth-check.js       # Проверка сессии и прав
└── pages/              # Вынесенная логика страниц
    ├── timeline.js         # Шахматка (~84KB)
    ├── preliminary.js      # Распределение (~99KB)
    ├── retreat-guests.js   # Гости ретрита (~76KB)
    ├── kitchen-menu.js     # Меню кухни (~71KB)
    ├── person.js           # Профиль вайшнава (~66KB)
    ├── stock-requests.js   # Заявки склада (~54KB)
    ├── bookings.js         # Бронирования (~39KB)
    └── departures.js       # Выезды (~38KB)

css/
└── common.css          # Общие стили, --current-color

guest-portal/js/
├── portal-auth.js      # Авторизация гостей
├── portal-data.js      # Загрузка данных
├── portal-index.js     # Логика dashboard
└── portal-layout.js    # Header/Footer/i18n
```

### Паттерны

1. **Layout.init()** — инициализация на каждой странице
2. **Layout.t(key)** — получение перевода
3. **Layout.db** — Supabase клиент
4. **Layout.handleError()** — единая обработка ошибок
5. **Cache.getOrLoad()** — кэширование с TTL

---

## Статистика

| Метрика | Значение |
|---------|----------|
| HTML страниц | 78 |
| SQL-миграций | 107+ |
| Таблиц БД | 40+ |
| RLS политик | 82 (после очистки) |
| JS файлов | 30 (~18,700 строк) |
| Языков | 3 (RU/EN/HI) |
| Модулей | 4 (Kitchen, Housing, CRM, Admin) |
| Рецептов | 129 |
| Категорий рецептов | 12 |

---

*Документ создан: 31 января 2026*
*Обновлён: 4 февраля 2026*
*Версия: 3.0*
