# Раздел "Гости" для ШРСК

## Решения

| Вопрос | Решение |
|--------|---------|
| Структура БД | Нормализованная (7 таблиц + изменения в retreats) |
| История участия | Да, один гость → много ретритов |
| Заметки о госте | Таблица `guest_notes`, привязка к ретриту или общие |
| Несколько платежей | Да, но пока текстовое поле |
| Импорт из Google Sheets | Клиентский JS, временное решение |
| Будущее | Своя анкета на сайте → напрямую в БД |
| Идентификация гостя | Система баллов (email, телефон, имя...) |
| Формат телефонов | E.164 (+79261234567) |
| Имена колонок CSV | Фиксированные |
| Конфликт данных | Спрашиваем пользователя |
| Семьи | family_group_id, объединение вручную |
| Список гостей | Таблица (не карточки) |
| Редактирование | Модалка |
| Удаление | Мягкое (is_deleted) |
| Поиск | Имя, духовное имя, страна |
| Экспорт | Не нужен |

---

## Структура БД

### Таблица 1: `guests` — Профиль гостя

Постоянная информация (не зависит от ретрита).

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| first_name | text | Имя |
| last_name | text | Фамилия |
| spiritual_name | text | Духовное имя (nullable) |
| birth_date | date | Дата рождения |
| gender | text | male / female |
| country | text | Страна проживания |
| phone | text | Телефон E.164 (+79261234567) |
| email | text | Email |
| telegram | text | Telegram username |
| photo_url | text | URL фото |
| india_experience | text | Опыт поездок в Индию |
| notes | text | Общие заметки |
| family_group_id | uuid | Группа семьи (nullable) |
| is_deleted | boolean | Мягкое удаление (default: false) |
| created_at | timestamptz | |
| updated_at | timestamptz | |

### Таблица 2: `retreat_registrations` — Регистрация на ретрит

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| guest_id | uuid | FK → guests |
| retreat_id | uuid | FK → retreats |
| registration_date | date | Дата подачи анкеты |
| status | text | pending / confirmed / team / cancelled / no_response |
| companions | text | С кем едет (имена) |
| accommodation_wishes | text | Пожелания по проживанию |
| meal_type | text | prasad / self / child |
| payment_notes | text | Заметки по оплате (пока текст) |
| org_notes | text | Заметки организаторов |
| is_deleted | boolean | Мягкое удаление (default: false) |
| created_at | timestamptz | |
| updated_at | timestamptz | |

### Таблица 3: `guest_visas` — Визы

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| registration_id | uuid | FK → retreat_registrations |
| application_id | text | Номер заявки на визу |
| status | text | pending / approved / rejected |
| notes | text | Заметки |
| created_at | timestamptz | |

### Таблица 4: `guest_payments` — Платежи (на будущее)

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| registration_id | uuid | FK → retreat_registrations |
| payment_date | date | Дата платежа |
| amount | decimal | Сумма |
| currency | text | RUB / EUR / USD |
| payment_method | text | tbank / paypal / cash |
| description | text | За что платёж |
| notes | text | Заметки |
| created_at | timestamptz | |

### Таблица 5: `guest_accommodations` — Размещение

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| registration_id | uuid | FK → retreat_registrations |
| room_id | uuid | FK → rooms (nullable) |
| building_name | text | ШРСК / Бхадур / БВ |
| room_number | text | Номер комнаты |
| room_type | text | 1x / 2x / 3x / 4x |
| check_in_date | date | Дата заезда |
| check_in_time | time | Время заезда |
| check_out_date | date | Дата выезда |
| check_out_time | time | Время выезда |
| notes | text | Заметки |
| created_at | timestamptz | |

### Таблица 6: `guest_transfers` — Трансферы

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| registration_id | uuid | FK → retreat_registrations |
| direction | text | arrival / departure |
| flight_datetime | timestamptz | Дата и время рейса |
| flight_number | text | Номер рейса |
| needs_transfer | text | yes / no / custom |
| transfer_group | text | Группа машин |
| seats | int | Мест в машине |
| price | decimal | Цена |
| notes | text | Заметки |
| created_at | timestamptz | |

### Таблица 7: `guest_notes` — Заметки о гостях

| Поле | Тип | Описание |
|------|-----|----------|
| id | uuid | PK |
| guest_id | uuid | FK → guests |
| registration_id | uuid | FK → retreat_registrations (nullable) |
| note | text | Текст заметки |
| created_by | uuid | FK → profiles (кто написал) |
| created_at | timestamptz | |

- `registration_id` заполнен → заметка к конкретному ретриту
- `registration_id` = null → общая заметка о госте

### Изменение существующей таблицы `retreats`

```sql
-- Временно (импорт из Google Sheets)
google_sheet_url text

-- На будущее (своя анкета)
registration_open  boolean  -- открыта ли регистрация
registration_start date     -- дата начала регистрации
registration_end   date     -- дата окончания регистрации
```

---

## Диаграмма связей

```
guests ←──────── retreat_registrations ────────→ retreats
   │                     │
   │           ┌─────────┼─────────┬─────────┐
   │           │         │         │         │
   │           ▼         ▼         ▼         ▼
   │     guest_visas  payments  accommodations  guest_notes
   │           │                      │              │
   │           │                      ▼              │
   │           │                 transfers           │
   │           │                                     │
   │           └──────────────── guest_notes ────────┘
   │                          (к регистрации или общие)
   │
   └──── family_group_id (самосвязь для семей)
```

---

## Справочники

### Статусы регистрации
| Код | Русский |
|-----|---------|
| pending | Планирует |
| confirmed | Точно едет |
| team | Команда |
| cancelled | Отказался |
| no_response | Не отвечает |

### Типы питания
| Код | Русский |
|-----|---------|
| prasad | Прасад |
| self | Самостоятельно |
| child | Детское |

### Способы оплаты (на будущее)
| Код | Название |
|-----|----------|
| tbank | Т-банк |
| paypal | PayPal |
| cash | Наличные |

### Валюты
RUB (₽), EUR (€), USD ($)

---

## Импорт из Google Sheets

### Как это работает

1. Google таблица публикуется как CSV
2. URL сохраняется в `retreats.google_sheet_url`
3. На странице кнопка "Синхронизировать"
4. JS делает fetch → парсит → upsert в Supabase

> **Примечание:** Это временное решение. В будущем регистрация будет полностью в нашей системе.

### Маппинг колонок CSV → База

| Колонка CSV | → Поле |
|-------------|--------|
| Ваше имя и фамилия | `guests.first_name` + `last_name` |
| Духовное имя | `guests.spiritual_name` |
| Дата рождения | `guests.birth_date` |
| Пол | `guests.gender` |
| Страна проживания | `guests.country` |
| Телефон | `guests.phone` (нормализуем) |
| e-mail | `guests.email` |
| Телеграм | `guests.telegram` |
| Фото | `guests.photo_url` |
| Опыт в путешествиях | `guests.india_experience` |
| Дата Анкеты | `registrations.registration_date` |
| Статус заявки | `registrations.status` |
| С кем едет | `registrations.companions` |
| Пожелания | `registrations.accommodation_wishes` |
| Питание | `registrations.meal_type` |
| Отметки организаторов | `registrations.org_notes` |
| Application ID | `guest_visas.application_id` |
| Оплата (все колонки) | `registrations.payment_notes` |
| Проживание | `guest_accommodations.*` |
| Трансфер | `guest_transfers.*` |

### Идентификация гостя (система баллов)

| Поле | Баллы |
|------|-------|
| Email совпал | +5 |
| Телефон совпал | +4 |
| Духовное имя | +3 |
| Telegram | +3 |
| Имя + Фамилия | +2 |
| Дата рождения | +2 |

- **5+ баллов** → тот же гость, обновляем
- **< 5 баллов** → создаём нового
- **Несколько кандидатов** → показываем выбор

### Нормализация телефонов (E.164)

```javascript
function normalizePhone(phone) {
    if (!phone) return null;
    let clean = phone.replace(/[^\d+]/g, '');
    if (clean.match(/^8\d{10}$/)) {
        clean = '+7' + clean.slice(1);
    }
    if (!clean.startsWith('+')) {
        clean = '+' + clean;
    }
    return clean;
}
```

| Ввод | Результат |
|------|-----------|
| +7 (926) 123-45-67 | +79261234567 |
| 8-926-123-45-67 | +79261234567 |
| +380 97 110 61 68 | +380971106168 |

### При конфликте данных

Показываем полный контекст для принятия решения:

```
┌──────────────────────────────────────────────────────────────────┐
│ Найден похожий гость (8 баллов)                                 │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│ В БАЗЕ:                          В CSV:                         │
│ ─────────────────────────────    ─────────────────────────────  │
│ Таня Чуб                         Татьяна Чуб                    │
│ Тривени дд                       Тривени дд                     │
│ +31650268367                     +31650268367  ✓                │
│ tanya@mail.ru                    tanya_new@mail.ru  ⚠️          │
│ Нидерланды                       Нидерланды  ✓                  │
│ 16.05.1970                       16.05.1970  ✓                  │
│                                                                  │
│ История в базе:                                                  │
│ • Картика 2024 — ШРСК 2х05                                      │
│ • Бхагаватам 2023 — ШРСК 2х12                                   │
│                                                                  │
├──────────────────────────────────────────────────────────────────┤
│ ○ Это тот же человек — обновить данные                          │
│ ○ Это другой человек — создать нового                           │
│ ○ Пропустить эту строку                                         │
│                                                                  │
│ [Применить]                                                      │
└──────────────────────────────────────────────────────────────────┘
```

**Что показываем:**
- Все основные поля рядом (база vs CSV)
- Отметки ✓ / ⚠️ где совпало / различается
- История ретритов из базы (главный контекст!)
- Баллы совпадения

---

## История гостя

Карточка гостя показывает всю историю участия:

```
┌─────────────────────────────────────────────────────────┐
│ Таня Чуб (Тривени дд)                                  │
│ Нидерланды · +31650268367 · tanya@mail.ru              │
├─────────────────────────────────────────────────────────┤
│ История ретритов:                                       │
│                                                         │
│ ▼ Бхагаватам ретрит 2025                               │
│   Комната: ШРСК 2х01 (с Ананда-дайини)                 │
│   Статус: Подтверждён                                   │
│   📝 "Приехала раньше на день" — Админ, 04.08.2025     │
│                                                         │
│ ▼ Картика 2024                                          │
│   Комната: ШРСК 2х05                                   │
│   📝 "Помогала на кухне" — Мария, 15.10.2024           │
│   📝 "Отличный повар!" — Алексей, 20.10.2024           │
│                                                         │
│ ▼ Общие заметки                                         │
│   📝 "Вегетарианка 20 лет" — Админ, 01.01.2024         │
│                                                         │
│ [+ Добавить заметку]                                    │
└─────────────────────────────────────────────────────────┘
```

Данные берутся из:
- `retreat_registrations` — список ретритов
- `guest_accommodations` — где жил
- `guest_notes` — заметки (к ретриту или общие)

---

## Семьи

- Поле `guests.family_group_id` — UUID группы
- Все члены семьи имеют одинаковый ID
- Объединение — **вручную** в интерфейсе
- Кнопка "Объединить в семью" для выбранных гостей

---

## Интерфейс

### Страницы

| Страница | Описание |
|----------|----------|
| `housing/guests.html` | Список всех гостей |
| `housing/guest.html` | Карточка гостя (история участия) |
| `housing/retreat-guests.html` | Гости ретрита + импорт |

### Список гостей ретрита

- Отображение: **таблица**
- Редактирование: **модалка**
- Поиск: имя, духовное имя, страна
- Удаление: **мягкое** (is_deleted = true)

### Статистика ретрита

```
Всего: 85 | Подтверждено: 58 | Команда: 27 | Отказ: 3
```

---

## Будущее: Анкета на сайте

> Google Sheets — временный костыль. Целевое решение — своя форма регистрации.

### Как будет работать

```
Сейчас:     Google Sheets → [Импорт] → База данных
Потом:      Анкета на сайте ────────→ База данных
```

### Что нужно для своей анкеты

1. **Публичная страница** `register.html`
   - Выбор ретрита (из открытых для регистрации)
   - Форма с полями из таблицы `guests` + `retreat_registrations`
   - Валидация на клиенте и сервере

2. **Автоматический матчинг**
   - "Вы уже были у нас? Введите email"
   - Подтягиваем данные, гость только проверяет

3. **Поля в `retreats`** (уже добавлены в план)
   ```sql
   registration_open   boolean  -- открыта ли регистрация
   registration_start  date     -- начало регистрации
   registration_end    date     -- окончание
   ```

4. **Личный кабинет** (опционально, на будущее)
   - Гость видит свои регистрации
   - Статус визы, оплаты
   - История участия

---

## План реализации

### Шаг 1: Миграции БД

```
067_guests.sql
068_retreat_registrations.sql
069_guest_visas.sql
070_guest_payments.sql
071_guest_accommodations.sql
072_guest_transfers.sql
073_guest_notes.sql
074_retreats_update.sql  (google_sheet_url + поля регистрации)
```

### Шаг 2: Интерфейс

1. `housing/guests.html` — список гостей
2. `housing/retreat-guests.html` — гости ретрита + импорт
3. Модалка редактирования гостя
4. Функционал объединения в семью

### Шаг 3: Импорт из Google Sheets

1. Парсер CSV
2. Система матчинга по баллам
3. UI для разрешения конфликтов
4. Прогресс и лог импорта

---

## Верификация

1. Применить миграции
2. Проверить структуру таблиц
3. Импортировать тестовые данные из CSV
4. Проверить поиск, фильтрацию, редактирование
5. Проверить объединение в семью
6. Проверить добавление заметок (к ретриту и общих)
7. Проверить отображение истории гостя
