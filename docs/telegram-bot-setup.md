# Настройка Telegram бота @rupaseva_bot

## 1. Создание бота через @BotFather

### Шаг 1: Создать бота

```
/newbot
```

**Имя бота:** `Sri Rupa Seva Kunja Bot`
**Username:** `rupaseva_bot`

**BotFather вернёт:**
- Bot Token (сохранить в Supabase Secrets как `TELEGRAM_BOT_TOKEN`)

---

## 2. Настройка описания бота

### Команда /setdescription

**Описание (показывается при первом открытии бота):**

```
🙏 Добро пожаловать в Sri Rupa Seva Kunja Bot!

Этот бот отправляет уведомления о фотографиях с ваших ретритов:

📸 Новые фото загружены
🔍 AI нашёл вас на фотографиях

Чтобы начать получать уведомления, перейдите в Guest Portal и нажмите "Подключить Telegram".

Команды:
/help — справка
/stop — отключить уведомления
```

---

### Команда /setabouttext

**About (краткое описание, показывается в профиле бота):**

```
Уведомления о фото с ретритов Sri Rupa Seva Kunja и AI-поиск лиц
```

---

### Команда /setshortdescription

**Короткое описание (отображается в списке ботов):**

```
Фотогалерея с AI-распознаванием лиц
```

---

## 3. Настройка команд бота

### Команда /setcommands

**Список команд:**

```
start - Подключить уведомления (с токеном из Guest Portal)
help - Подробная справка о боте
stop - Отключить уведомления
```

---

## 4. Настройка изображения профиля (опционально)

### Команда /setuserpic

Загрузите изображение для аватара бота (рекомендуемый размер: 512x512 px)

**Идеи для изображения:**
- Логотип Sri Rupa Seva Kunja
- Символ камеры с лотосом
- Фото ашрама

---

## 5. Сохранение Bot Token в Supabase Secrets

### Dev окружение (vzuiwpeovnzfokekdetq)

```bash
# Через Supabase Dashboard:
Settings → Edge Functions → Secrets

# Добавить секрет:
TELEGRAM_BOT_TOKEN = <токен от BotFather>
```

### Production окружение (llttmftapmwebidgevmg)

```bash
# Через Supabase Dashboard:
Settings → Edge Functions → Secrets

# Добавить секрет:
TELEGRAM_BOT_TOKEN = <токен от BotFather>
```

---

## 6. Настройка Webhook для бота

После деплоя Edge Function `telegram-webhook` нужно зарегистрировать webhook.

### Команда для регистрации webhook:

```bash
# Dev
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://vzuiwpeovnzfokekdetq.supabase.co/functions/v1/telegram-webhook?secret=<WEBHOOK_SECRET>"

# Production
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://llttmftapmwebidgevmg.supabase.co/functions/v1/telegram-webhook?secret=<WEBHOOK_SECRET>"
```

**Где:**
- `<BOT_TOKEN>` — токен от BotFather
- `<WEBHOOK_SECRET>` — случайная строка (сохранить в Supabase Secrets как `TELEGRAM_WEBHOOK_SECRET`)

### Проверка webhook:

```bash
curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"
```

**Должен вернуть:**
```json
{
  "ok": true,
  "result": {
    "url": "https://vzuiwpeovnzfokekdetq.supabase.co/functions/v1/telegram-webhook?secret=...",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "max_connections": 40
  }
}
```

---

## 7. Тестирование бота

### Тест 1: Welcome message (без токена)

1. Найти бота в Telegram: `@rupaseva_bot`
2. Отправить `/start`
3. Должно прийти welcome message с инструкцией

### Тест 2: Подключение через Guest Portal

1. Зайти в Guest Portal
2. Нажать "Подключить Telegram"
3. Перейти по ссылке `t.me/rupaseva_bot?start=TOKEN`
4. Должно прийти сообщение "Уведомления подключены!"
5. В Guest Portal должен появиться статус "Подключён ✓"

### Тест 3: Команда /help

1. Отправить `/help` боту
2. Должна прийти подробная справка

### Тест 4: Отключение

1. Отправить `/stop` боту
2. Должно прийти сообщение "Уведомления отключены"
3. В Guest Portal статус должен измениться на "Не подключён"

### Тест 5: Уведомление о новых фото

1. Загрузить фото через `photos/upload.html` (как фотограф)
2. После индексации должны прийти уведомления всем подключённым участникам ретрита

---

## 8. Дополнительные настройки (опционально)

### Privacy Settings (/setprivacy)

**Рекомендация:** `DISABLED`

Это позволяет боту видеть сообщения в групповых чатах (если в будущем бот будет добавлен в группу ретрита).

### Inline Mode (/setinline)

**Не используется** в текущей версии.

### Allow Groups (/setjoingroups)

**Рекомендация:** `ENABLED`

На будущее, если бот будет использоваться в групповых чатах ретритов.

---

## 9. Мониторинг и логи

### Просмотр логов Edge Function

```bash
# Через Supabase Dashboard:
Edge Functions → telegram-webhook → Logs
```

### Проверка статистики бота

Telegram Bot API не предоставляет встроенную аналитику, но можно отслеживать:
- Количество подключений (SELECT COUNT(*) FROM vaishnavas WHERE telegram_chat_id IS NOT NULL)
- Логи отправки уведомлений (Edge Function logs)

---

## 10. Секреты в Supabase

**Полный список секретов для Edge Functions:**

| Секрет | Описание | Где используется |
|--------|----------|------------------|
| `TELEGRAM_BOT_TOKEN` | Токен от @BotFather | telegram-webhook, send-notification |
| `TELEGRAM_WEBHOOK_SECRET` | Случайная строка для защиты webhook | telegram-webhook |
| `AWS_ACCESS_KEY_ID` | AWS credentials для Rekognition | index-faces, search-face |
| `AWS_SECRET_ACCESS_KEY` | AWS credentials для Rekognition | index-faces, search-face |
| `AWS_REGION` | ap-south-1 (Mumbai) | index-faces, search-face |

---

## 11. Troubleshooting

### Бот не отвечает на /start

**Проверить:**
1. Webhook зарегистрирован: `curl "https://api.telegram.org/bot<BOT_TOKEN>/getWebhookInfo"`
2. Edge Function задеплоена: `supabase functions list --project-ref <PROJECT_ID>`
3. Секреты заданы: Supabase Dashboard → Edge Functions → Secrets
4. Логи Edge Function: ошибки в консоли

### Уведомления не приходят

**Проверить:**
1. `telegram_chat_id` сохранён в БД: `SELECT telegram_chat_id FROM vaishnavas WHERE id = '<VAISHNAVA_ID>'`
2. Пользователь не заблокировал бота (403 error в логах)
3. Право `upload_photos` у фотографа (для broadcast)
4. Логи Edge Function `send-notification`

### Webhook не работает после деплоя

**Решение:**
```bash
# Удалить старый webhook
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/deleteWebhook"

# Зарегистрировать новый
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=<NEW_URL>"
```

---

## 12. Контакты поддержки

**Telegram support channel:** `@rupaseva_support` (указан в /help)

**Для администраторов:** логи в Supabase Dashboard
