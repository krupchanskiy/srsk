# Результаты проверки — 25 июля 2026

Ведётся по плану [test_master_plan.md](test_master_plan.md). Всё, что пишет в
журнал операций, выполнялось в `BEGIN … ROLLBACK` — прод не изменён.

---

## Б1. Стык CRM → финмодуль — закрыта

### Найдено: удвоение оплат участников на cutover 🔴

Симуляция загрузки авансов на живых данных дала **77 сделок, 993 827 ₹ → 1 987 654 ₹**.
Ровно вдвое, все до единой. Последствия, если бы выстрелило 1 августа: у каждого
гостя оплата вдвое больше настоящей, долги обнуляются, портал и отчёт по ретриту
показывают неверные суммы.

**Причина 1 — формула сделки** (`crm_calc_deal_totals`). Складывались две величины,
описывающие одни и те же деньги:
- исторические подтверждённые CRM-платежи без операции в учёте;
- начальные остатки участника.

До запуска это работало, потому что остатков нет. Но генератор авансов создаёт их
из тех же платежей, а платежи остаются без операции — автопроводка пропускает их
как `pre_cutover`. Исправлено миграцией 269: если по сделке загружен остаток, её
исторические платежи больше не складываются.

**Причина 2 — загрузчик авансов** (`fin_load_crm_advances`). Забирал все
подтверждённые платежи до даты запуска, не проверяя, нет ли по ним операции в
журнале. Такой платёж есть: подтверждён до появления защиты по дате (миграция 241)
и успел пройти автопроводкой, 27 600 ₹. Исправлено миграцией 270: проведённые
платежи в остатки не берём, сторнированные — берём.

**Почему штатные проверки молчали.** `totals_mismatch` сверяет сохранённое значение
с формулой. Ошибка была в самой формуле — обе стороны согласны на удвоенном числе.
Слепая зона по устройству проверки.

**Добавлены два сторожа** (миграция 271), смотрящие на источники, а не на
согласованность:
- `advance_double_representation` — платёж есть и в журнале, и в остатке своей сделки;
- `advance_partial_load` — у сделки есть остаток, но сумма платежей с ним не сходится.

Проверок стало 14, все зелёные. После починки симуляция сходится до копейки.

### Прогон сценариев

| № | Сценарий | Итог |
|---|---|---|
| 1 | платёж, полученный до 1 августа, в учёт не попадает | ✅ `pre_cutover` |
| 2 | платёж после запуска проводится, сделка пересчитана | ✅ |
| 3 | повторное подтверждение не задваивает деньги | ✅ одна операция, одна проводка |
| 4 | комиссия банка: гостю полное, на счёт фактическое | ✅ 5000 / 4850 |
| 5 | доллары на рупиевый счёт — отказ, денег нет | ✅ `currency_mismatch` |
| 6 | удаление проведённого платежа запрещено | ✅ `payment_posted_to_finance` |
| 7 | правка суммы проведённого платежа запрещена | ✅ `payment_posted_immutable` |
| 8 | сторно проведённого платежа выполняется | ✅ |
| 9 | после сторно на счёте ноль | ✅ |
| 10 | повторное подтверждение после сторно денег не возвращает | ✅ `already_reversed` |
| 11 | второе сторно одной операции невозможно | ✅ `operation_already_reversed` |
| 12 | подтверждение без счёта запрещено | ✅ `confirm_requires_account` |
| 13 | подтверждение без суммы зачисления запрещено | ✅ `confirm_requires_amount_received` |

### Замечания без дефекта

**Порядок триггеров.** На `crm_payments` их 11. `crm_payments_recalc` срабатывает
раньше автопроводки, поэтому в момент пересчёта проводки ещё нет — но
`fin_trg_resync_deal_totals` на `fin_postings` доводит суммы после. Самолечение
работает, `totals_mismatch` зелёная.

**Автопроводка не блокирует оператора.** Тело обёрнуто в `EXCEPTION WHEN OTHERS`:
если проводка не удалась, платёж остаётся подтверждённым в CRM, а ошибка уходит в
журнал автопроводки и красным в утреннюю сводку. Осознанный выбор, страховка на
месте.

**Гвард подтверждения не действует без авторизации.** `crm_guard_payment_confirmation`
начинается с `IF auth.uid() IS NULL THEN RETURN NEW`. То есть операции под
сервисным ключом (миграции, Edge Functions) проходят без проверок. Для миграций это
нужно, но стоит помнить: скрипт может подтвердить платёж без счёта. Ловится
проверкой `unposted_payments`.

---

## Ожидает доступа

- **Конкурентность** — нужна строка подключения к БД (гонки двух фин-админов).
- **Снимок перед cutover** — проверить, включён ли PITR.

---

## Б-конк. Конкурентность — контур открыт

Доступ получен: прямое соединение Supabase теперь только по IPv6, поэтому
подключение идёт через **session pooler** (`aws-1-ap-south-1.pooler.supabase.com:5432`).
Строка лежит в `.env.local` (в `.gitignore`), тесты — два параллельных соединения
на Node.

### Найдено: гонка при автосоздании подотчётного счёта 🟡

`fin_dept_account` делал `SELECT`, потом `INSERT`. Два одновременных проведения
заявок в валюте, которой у департамента ещё нет, шли в `INSERT` оба.

Дубля не возникало — спасала уникальность имени активного счёта. Но проигравший
получал `duplicate key value violates unique constraint` вместо счёта, и заявка
оставалась непроведённой с невнятной ошибкой. Воспроизведено: второй блокировался
и падал.

Исправлено миграцией 272: блокировка на департамент + перечитывание счёта при
конфликте. После починки проигравший получает **тот же** счёт, ошибки нет.

### Прогон

| Гонка | Итог |
|---|---|
| два запроса счёта департамента в новой валюте | ✅ после 272: один счёт, оба получают его |
| двое проводят одну заявку из чата | ✅ второй ждёт, дубля операции нет |
| двое сторнируют одну операцию | ✅ второй ждёт |

### Замечание по методике

Первый прогон с `statement_timeout=0` и `lock_timeout=0` завис на три минуты и был
убит по таймауту. Открытых транзакций на проде не осталось (соединения закрылись,
всё откатилось), целостность после — зелёная. Причину зависания установить не
удалось: сессионных advisory-блокировок в коде нет, а с ограниченными лимитами
(`lock_timeout=10s`) оба сценария проходят штатно и повторно не воспроизводятся.
**Вывод для методики: конкурентные тесты гонять только с ограниченным
`lock_timeout`** — иначе висящая транзакция на проде блокирует живых пользователей.

Побочно: тест дважды оставлял мусор на проде (проведённую заявку и операцию на
700 ₹). Убрано штатным сторно — реестр append-only, обе записи остались, остаток
подотчёта гест-хауса снова 0. Все 14 проверок зелёные.

---

## Б2. Telegram-бот — закрыта, 78 проверок

Разбор текста тестируется **извлечением настоящих функций из `index.ts`**, а не
копией правил: копия неизбежно разошлась бы с кодом.

### Найдено: минус на подотчёте был невидим 🟡

`tg_on_negative_balance` выходил на `kind <> 'real'` — следил только за кассами и
банками. Подотчёты департаментов (`custodial`) в сигнал не попадали, а утренняя
сводка тоже перебирает только `real`-счета.

Проверено: расход 5000 при нулевом подотчёте Гест-хауса **прошёл**, счёт стал
−5000, и об этом не сообщил никто. Минус на подотчёте значит либо забытую выдачу,
либо перерасход — и то и другое надо видеть сразу.

Исправлено миграцией 273: сигнал покрывает подотчёты (со своей формулировкой —
«Похоже, выдача под отчёт не записана»), плюс в сводке появилась строка
«подотчёты в минусе». Скрытые счета по-прежнему молчат.

### Найдено: ноль проходил в заявку 🟢

Проверка «сумма положительная» стояла только в последней ветви разбора.
`«Купил овощи 0 ₹»` доходило до заявки, там ограничение БД роняло вставку, и бот
**молча не отвечал вообще**. Теперь одна проверка на все ветви.

### Прогон

| Группа | Проверок | Итог |
|---|---|---|
| разбор текста (3 словаря, суммы, мусор, валюты, наличные, описание) | 56 | ✅ все |
| безопасность вебхука (без секрета, неверный, пустой, GET, мусор) | 5 | ✅ все 403 |
| защиты базы (чужой чат, ноль, минус, дубль, чужой вид, чужая валюта) | 7 | ✅ все |
| денежные пути (минус, передача себе, непринятая заявка) | 3 | ✅ все |
| жизненный цикл (сутки, две подряд, не-админ ×2, отключённый чат, повтор) | 7 | ✅ все |

Отдельно проверено на злонамеренном вводе: HTML-теги, амперсанд, 4000 символов,
огромные числа, минус, деванагари, латиница — ничего не ломает и не проходит
мимо правил.

### Ответ на открытый вопрос

**Расход при нулевом подотчёте разрешён** — счёт уходит в минус, операция
проходит. Это разумно (реальность не всегда по порядку), и теперь об этом
сообщает сигнал. Если правило должно быть строже — блокировать, — скажи.

---

## Б5. Права и приватность — частично

### Найдено: гость мог увидеть чужой баланс 🔴

`portal_fin_get_my_finances` определяла «кто я» через
`SELECT id FROM vaishnavas WHERE user_id = auth.uid() LIMIT 1` **без сортировки**.
Один вход делят несколько карточек: таких входов **17**, один делят **93 человека**
(групповая бронь).

Сегодня утечка скрытая — ни на одном общем входе нет двух человек с финансовыми
данными по одному ретриту. Но симуляция загрузки авансов показала: **1 августа
таких пар станет четыре**, и ни одна не связана через `family_links`, то есть по
правилам самой системы видеть чужое им нельзя:

| Пара | Общая почта |
|---|---|
| Гаури прия дд · Лалита лила дд | gintare.gudeliene@… |
| Ангелина Трунина · Ольга Библис | oljabiblis@… |
| Аруна Гауранга Дас · Георгета Фолештян | artiom.foleshteanu@… |
| Адхиягья дас · Анастасия Еронина | govinda108s@… |

Вдобавок выбор человека был **случайным**: баланс мог меняться между открытиями
страницы.

Исправлено миграцией 274: при неоднозначном входе портал не показывает финансы
и объясняет почему. Показать чужой баланс хуже, чем не показать никакого.
Настоящее лечение — отдельный вход каждому; новая проверка
`shared_login_blocks_portal` показывает, скольких это задевает (сейчас 0, после
cutover будет 4).

Заодно удалена `fin_actor_contact_id` — мёртвый код с той же ловушкой.

### Периметр скрытых счетов («Подушка») — 9 из 9

| Проверка | Итог |
|---|---|
| фин-админ без доступа: функция доступа | ✅ не видит |
| остатки счетов / лента / сверки / доступы | ✅ нигде нет |
| допущенный видит и в функции, и в остатках | ✅ |
| исключена из выборки утренней сводки | ✅ |
| исключена из новой строки про минусы подотчётов | ✅ |

### Хорошая новость про суперпользователей

`fin_is_admin` **не** обходится флагом суперпользователя: из 20 суперпользователей
фин-админов только 5. Финансовый периметр не протекает через суперправа.

### Сводка после двух правок

Проверено целиком в откате (письмо не ушло): обе новые строки собираются —
«🔴 подотчёт в минусе — Кафе (−1 500 ₹)» и «в чатах без ответа — 4 карточки».

---

## Итог дня

**Дефектов найдено: 7.** Из них критичных 2 (удвоение оплат на 993 тысячи,
чужой баланс в портале), существенных 3, мелких 2. Все починены.
Миграции 269–274, обработчик бота v17.

Проверок целостности было 12 → стало **15**, все зелёные.
Прод после всех прогонов чист: тестовые операции сторнированы, заявки удалены.

---

## Б4. Ядро учёта — разрушающее тестирование

### Найдено: дата в будущем уводила деньги из виду 🔴

Расход с датой 2027 года **принимался**. Проверено в откате: пополнили кассу на
50 000, списали 30 000 датой 2027-09-01 →

- остаток кассы стал **20 000 прямо сейчас** (остаток считается по всем проводкам,
  дата не учитывается);
- в отчёте за последний месяц этого расхода **нет вообще** (отчёты по периоду).

Одна опечатка в годе уводит деньги из кассы сегодня, не появляясь ни в одном
отчёте. При сверке касса не сходится, и объяснить разницу нечем.

Исправлено миграцией 275: дата позже `CURRENT_DATE + 1` отклоняется. Запас в день
обязателен — база в UTC, ашрам в Мумбаи (+5:30), местное «сегодня» бывает
завтрашним по UTC. Задним числом по-прежнему можно: нужно для «забыли записать».
Добавлен инвариант `operation_in_future`.

Проверены все четыре границы: 2027 ✅ отклонено, +1 ✅ прошло, +2 ✅ отклонено,
−200 дней ✅ прошло.

### Прогон

| Что подавалось | Итог |
|---|---|
| ноль, минус | ✅ `invalid_payload` |
| меньше копейки (0.001) | ✅ отклонено (код `internal_error` — сообщение бесполезное) |
| несуществующий счёт | ✅ `account_not_found` |
| закрытый счёт | ✅ `account_inactive` |
| посторонний ключ в запросе | ✅ `invalid_payload` |
| перевод с разными суммами в одной валюте | ✅ `invalid_payload` |
| перевод счёта самому себе | ✅ `invalid_payload` |
| **тот же request_id с другой суммой** | ✅ `idempotency_conflict`, денег не добавилось |
| тот же request_id с тем же запросом | ✅ одна операция, повтор успешен |

### Признание по методике

Первый прогон дал три результата **«по неверной причине»**: касса была пуста, и
расходы падали на нехватке средств, а не на проверяемом условии. Идемпотентность
выглядела пройденной, хотя не проверялась вовсе. После пополнения счёта вылез
настоящий дефект с датой. **Вывод: в такие батареи сначала кладём деньги** —
иначе тесты зеленеют, ничего не проверяя.

---

## Б3. Cutover — репетиция пройдена

### Снимок внутри базы (миграции 276–277)

PITR не включён (платный аддон), ежедневный бэкап — около 05:05 по Мумбаи.
Сделан точный снимок: копия ровно тех 13 таблиц, что стирает reset, плюс
значение боевой последовательности и метки `report_dirty_at`.

```
SELECT fin_cutover_snapshot();                        -- перед reset
SELECT fin_cutover_snapshot_info();                   -- проверить
SELECT fin_cutover_restore('ВОССТАНОВИТЬ ИЗ СНИМКА'); -- если понадобится
```

В `fin_cutover_reset.sql` добавлен шаг 0: **скрипт не запустится без снимка** —
падает с подсказкой. Иначе откатываться было бы некуда.

Ограничение: снимок живёт в той же базе. Спасает от «reset прошёл, а данные
оказались нужны», но не от потери базы — для этого остаётся бэкап Supabase.

### Три дефекта, найденные самой репетицией

Все три — в моём же коде возврата, все вылезли на живых данных:

1. **identity-колонка.** У `fin_audit_log.id` тип `GENERATED ALWAYS AS IDENTITY` —
   обычный `INSERT … SELECT` запрещён, возврат падал сразу. Теперь для таких
   таблиц добавляется `OVERRIDING SYSTEM VALUE`, а последовательность
   подтягивается к `max(id)`.
2. **Лишние записи в аудите (+7).** Возврат обновляет `report_dirty_at`, но аудит
   на `fin_accounting_objects` не отключался. Официальный reset его отключает — в
   возврате было пропущено.
3. **Суммы сделок не пересчитывались.** reset пересчитывает их на пустой журнал,
   а во время возврата триггер отключён. Добавлен пересчёт в конце.

### Итог репетиции

| Показатель | До | После возврата |
|---|---|---|
| операций | 157 | 157 ✅ |
| проводок | 223 | 223 ✅ |
| начислений | 15 | 15 ✅ |
| записей аудита | 583 | 583 ✅ |
| заявок из чатов | 16 | 16 ✅ |
| сумма проводок | 3 110 194,68 | совпало ✅ |
| `ledger_seq` | 389 | 389 ✅ |
| проверки целостности | — | все 16 зелёные ✅ |

Остаточная разница: `sum(total_paid)` даёт 1 117 427,05 против исходных
1 117 427,0437 — это округление, не потеря. Побочно выяснилось, что суммы сделок
пишут **два места с разным округлением** (`crm_recalc_deal_finances` и
`crm_calc_deal_totals`); проверка терпит 0,01, поэтому расхождение не всплывало.
Не дефект, но стоит однажды свести к одному writer'у.

---

## Батчи G1 и G2 — дочёт 20 сценариев ТЗ

Оставались 20 сценариев со статусом «дочек»: компоненты зелёные, но точную
формулировку сценария явно не прогоняли. Прогнаны все, кроме одного. Метод —
`BEGIN … ROLLBACK` на проде, роль через `set_config('request.jwt.claims', …)`,
ассерты в temp-таблицу. После прогонов прод проверен: тестовых следов нет,
все 16 проверок целостности зелёные.

**Итог матрицы: было ✅ 62 · 🔄 20, стало ✅ 86 · 🔄 1.**

### Что подтвердилось

| № | Сценарий | Результат |
|---|---|---|
| 6 | комиссия PayPal отдельной тратой | сторно платежа комиссию не тронуло, остаток −3.50 $ |
| 8 | цикл подотчёта | 10000 → 8000 → 2000 → 0 |
| 11 | эффективное направление затрат | явное перекрывает умолчание счёта |
| 18 | операция раньше чекпоинта сверки | принята |
| 21 | минус по счёту | подотчёт ушёл в −5000, реальный счёт отказал |
| 23 | выемка ящика | 4500 → 0, доход посчитан один раз |
| 28 | реестр + аванс CRM | реестр вытесняет платежи CRM, удвоения нет |
| 33 | отмена начисления по закрытому | без причины отказ, с причиной проходит |
| 35 | сторно наследует статью | одна статья на пару, знаковая сумма 0 |
| 40, 59 | перевод и начальный остаток вне статей | проводок со статьёй — 0 |
| 47 | повторный общий курс | перезапись, строка одна |
| 49 | повторный реестр | повтор `skip=1`; другая сумма → пачка отклонена целиком |
| 58 | отмена после закрытия | отчёт грязный, версия закрытия не выросла |
| 65 | долг за проживание | перенесён с видом «проживание» |
| 73, 74 | подсунутый `balance_kind` | отклонён как неизвестное поле |
| 76 | три валюты в одной операции | ₹+$+€, построчное округление, расхождений 0 |
| 82, 91 | чужой payload сверки | отказ в обе стороны |
| 83 | курс из будущего | на 20.07 взят курс 90, не 999 |
| 86 | операция по закрытому объекту | без причины отказ; с причиной — `is_post_close` и грязный отчёт |
| 104 | причина и `request_id` в аудите | по всем трём админским действиям |
| 105 | перенос проводки на другого участника | запрещён: аналитика до-закрытийных проводок неизменна |

Попутно закрыты три проверки ввода при пересчёте наличных: точка хранения
дважды, номинал не из справочника валюты, сумма «прочее» без комментария —
все три отклоняются с внятным текстом.

### Что осталось

**Сценарий 88** (read-lock-recheck объекта в возврате) — единственный незакрытый.
Нужны два параллельных соединения; контур K2 упирается в пароль базы.

### Два наблюдения не про код

**В проде нет ни одного непривилегированного финпользователя.** Право
`fin_account_user` без `fin_admin` не выдано никому. Цикл согласования
(`expense` не от админа → `pending`) в проде ещё ни разу не срабатывал —
проверить его удалось только на временном пользователе внутри отката.
Заведующие департаментами, которым предстоит работать с ботом, доступа
в финмодуль пока не имеют.

**Справочник направлений затрат пуст.** Механизм рабочий (проверено на
временных записях), но ни одного направления не заведено, поэтому вся
аналитика по направлениям сейчас пустая.

### Честно о самих тестах

Из первых прогонов пять раз красным светились не дефекты, а мои ошибки в
тестах: `id` строки должен быть UUID, знак проводки лежит в `direction`, а не
в знаке `amount`, `counts` — массив точек хранения, а не карта номиналов,
`fin_create_*` возвращают `{ok:false}` вместо исключения (проигнорированный
результат тихо ломал следующий шаг). Отдельно стоит того, что сценарии 73 и 74
в первом прогоне позеленели **ложно** — заявка отклонялась из-за плохого `id`,
а не из-за подсунутой аналитики. Перепроверены с валидным `id`.

---

## Батч G3 + пересчёт матрицы

Сводка в шапке программы не сходилась с таблицей — и до моих правок тоже
(писала 🔄 20 при 25 строках с этим статусом). Пересчитал по строкам: цифры
в шапке теперь получены из таблицы, а не написаны руками.

Прогнаны три сценария, которые числились непрогнанными, хотя двух соединений
не требуют:

| № | Сценарий | Результат |
|---|---|---|
| 64 | одна операция на закрытый и открытый объект | метка `is_post_close` проставлена построчно: у строки закрытого объекта да, у открытого нет |
| 93 | трата департамента по закрытому объекту | `object_closed`; причиной закрытие не обходится — она работает только у админа |
| 103 | перенос после-закрытийной проводки на открытый объект | метка снята, пересчёт корректный |

**Итог: ✅ 87 · 🔄 1 · ⬜ 9 · 📋 4 · 🚫 2 · ❗ 2.**

Все 9 непрогнанных и единственный дочёт — контур K2 (конкурентность), он
упирается в пароль базы: нужны два параллельных psql-соединения.
