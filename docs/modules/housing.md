# Модуль Housing (Проживание)

## Обзор

Модуль управления проживанием: люди, размещение, ресепшен.

- **Цвет модуля**: `#8b5cf6` (фиолетовый)
- **Layout.init**: `module: 'housing'`

---

## Структура файлов

```
vaishnavas/                   # База людей
├── index.html                # Все вайшнавы
├── guests.html               # Только гости
├── team.html                 # Только команда
├── person.html               # Карточка человека
├── preliminary.html          # Распределение гостей
└── retreat-guests.html       # Импорт гостей ретрита (CSV)

placement/                    # Размещение
├── timeline.html             # Шахматка (timeline)
├── bookings.html             # Бронирования
├── arrivals.html             # Заезды
├── departures.html           # Выезды
└── transfers.html            # Трансферы

reception/                    # Ресепшен
├── floor-plan.html           # Планы этажей
├── floor-plan-editor.html    # Редактор планов
├── cleaning.html             # Уборка
├── buildings.html            # Здания
├── rooms.html                # Комнаты
└── dictionaries.html         # Справочники
```

---

## Вайшнавы (vaishnavas/)

### index.html — Все вайшнавы

**Функционал:**
- Таблица всех людей
- Поиск по имени, email, телефону
- Сортировка по колонкам
- Фильтр: все / команда / гости
- Добавление нового человека
- Индикатор присутствия

**Таблицы БД:**
- `vaishnavas`
- `departments`
- `vaishnava_stays`

**Утилиты:** `VaishnavasUtils` (js/vaishnavas-utils.js)

---

### guests.html — Гости

**Функционал:**
- Только гости (is_team_member = false)
- То же что index.html, но отфильтровано

---

### team.html — Команда

**Функционал:**
- Только команда (is_team_member = true)
- Отдел и служение
- Старший (senior)
- Периоды пребывания

---

### person.html — Карточка человека

**URL параметры:** `?id={vaishnava_id}`

**Функционал:**
- Просмотр/редактирование профиля
- Фото
- Контактные данные
- Духовная информация (учитель, инициация)
- Для команды: отдел, служение, старший
- История проживаний
- Регистрации на ретриты

**Режимы:** view-mode / edit-mode

**Таблицы БД:**
- `vaishnavas`
- `departments`
- `spiritual_teachers`
- `residents`
- `retreat_registrations`

---

### preliminary.html — Распределение

**Функционал:**
- Таблица гостей ретрита
- Назначение здания и комнаты
- Цветовая индикация:
  - 🟢 Зелёный — заселен
  - 🔴 Красный — самостоятельно
  - ⚪ Без цвета — не распределён
- Заметки (локальные, localStorage)
- Сортировка с пустыми в конце

**Таблицы БД:**
- `retreat_registrations`
- `vaishnavas`
- `residents`
- `buildings`
- `rooms`

---

### retreat-guests.html — Импорт CSV

**Функционал:**
- Импорт гостей из CSV
- Автоматический матчинг:
  - Email (+5 баллов)
  - Телефон (+4)
  - Духовное имя (+3)
  - Telegram (+3)
- Разрешение конфликтов (score 3-4)
- Парсинг дат разных форматов

**Форматы дат:**
```
"7 февраля 18:30"
"22.02.26 5:50"
"06.02.2026 в 04.05"
"07.03.2026"
```

**Очистка полей:**
- Духовное имя: "нет", "пока нет", "-" → пустое
- Страна+город: "Латвия, Рига" → разделяется

---

## Размещение (placement/)

### timeline.html — Шахматка

**Функционал:**
- Визуальная сетка: комнаты × даты
- Бронирования как блоки
- Drag & drop (планируется)
- Переключение периода

**Realtime:** Подписка на `residents`, `bookings`

**Таблицы БД:**
- `rooms`
- `buildings`
- `residents`
- `bookings`

---

### bookings.html — Бронирования

**Функционал:**
- Список бронирований
- Создание/редактирование
- Статусы бронирования

**Таблицы БД:**
- `bookings`
- `rooms`
- `vaishnavas`

---

### arrivals.html — Заезды

**Функционал:**
- Список прибывающих гостей
- Время прилёта
- Расчётное время приезда (прилёт + 4 часа)
- Статус такси (нужно / заказано)
- Информация о размещении

**Колонки:**
| Колонка | Описание |
|---------|----------|
| Гость | Имя + контакт |
| Прилёт | Время + бейдж такси |
| Приедет ≈ | Прилёт + 4 часа |
| Размещение | Здание + комната |

**Бейджи такси:**
- 🟡 `badge-warning` — нужен трансфер
- 🟢 `badge-success` — такси заказано (кликабельный)

**Таблицы БД:**
- `guest_transfers` (direction = 'arrival')
- `retreat_registrations`
- `residents`
- `vaishnavas`

---

### departures.html — Выезды

**Функционал:**
- Список выезжающих
- Время выезда (вылет - 7 часов)
- Время вылета
- Статус такси

**Таблицы БД:**
- `guest_transfers` (direction = 'departure')

---

### transfers.html — Трансферы

**Функционал:**
- Все трансферы (заезды + выезды)
- Заказ такси
- Информация о водителе
- Кто заказал

**Модалка такси:**
- Информация о водителе (textarea)
- Кто заказал (input)
- Кнопки: Сохранить, Отменить такси

---

## Ресепшен (reception/)

### floor-plan.html — Планы этажей

**Функционал:**
- Визуальные планы зданий (SVG)
- Статус комнат (занято/свободно)
- Кликабельные комнаты → информация

**Таблицы БД:**
- `floor_plans`
- `buildings`
- `rooms`
- `residents`

---

### floor-plan-editor.html — Редактор планов

**Функционал:**
- Загрузка SVG
- Разметка комнат на плане
- Привязка к данным комнат

---

### cleaning.html — Уборка

**Функционал:**
- Расписание уборки
- Статусы: нужна / в процессе / готово
- Назначение ответственных
- История уборок

**Realtime:** Подписка на `room_cleanings`

**Таблицы БД:**
- `room_cleanings`
- `cleaning_tasks`
- `rooms`
- `vaishnavas`

---

### buildings.html — Здания

**Функционал:**
- Справочник зданий
- Тип здания
- Временные здания (даты доступности)
- Порядок сортировки
- Цвет здания

**Таблицы БД:**
- `buildings`
- `building_types`

**Временные здания:**
```
is_temporary = true, dates = NULL     → Доступно всегда
is_temporary = true, dates = set      → Доступно в период
is_temporary = false                  → Постоянное
```

---

### rooms.html — Комнаты

**Функционал:**
- Справочник комнат
- Здание, этаж, тип
- Вместимость
- Удобства

**Таблицы БД:**
- `rooms`
- `room_types`
- `buildings`

---

### dictionaries.html — Справочники

**Функционал:**
- Типы зданий
- Типы комнат
- Категории проживающих

**Таблицы БД:**
- `building_types`
- `room_types`
- `resident_categories`

---

## Ключевые таблицы

### vaishnavas
```
id, user_id, spiritual_name, first_name, last_name
email, phone, telegram, whatsapp
country, city, birth_date
spiritual_teacher, initiation_date
is_team_member, department_id, service, senior_id
photo_url, notes
```

### residents
```
id, vaishnava_id, room_id, booking_id
check_in, check_out
category_id, notes
```

**room_id = NULL** означает "Самостоятельно" (гость живёт вне ашрама)

### guest_transfers
```
id, vaishnava_id, retreat_id
direction ('arrival' | 'departure')
flight_datetime
needs_transfer ('yes' | 'no')
taxi_status ('ordered' | NULL)
taxi_driver_info, taxi_ordered_by
```

---

## Утилиты

### VaishnavasUtils (js/vaishnavas-utils.js)

```javascript
// Рендер строки таблицы
VaishnavasUtils.renderPersonRow(person, options)

// Открыть модалку добавления
VaishnavasUtils.openAddModal()

// Загрузка пребываний
const stays = await VaishnavasUtils.loadStays()

// Проверка присутствия
VaishnavasUtils.isPresent(personId, stays)
```

---

## Связанная документация

- [Архитектура](../architecture.md)
- [База данных](../database.md)
- [Паттерны](../patterns.md)
